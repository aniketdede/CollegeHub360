# Application runtime (fill locally or in the deployment platform; never commit real values)
# PostgreSQL-compatible database used by db/schema.sql. Leave blank to keep account routes disabled locally.
DATABASE_URL=
# Generate with: openssl rand -base64 32. Keep server-side only; never expose as NEXT_PUBLIC_.
SESSION_SECRET=
REDIS_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000
